{
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run -- clean",
    "build": "lerna run -- build",
    "deploy": "npm run clean && npm run build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^2.7.0"
  }
}
